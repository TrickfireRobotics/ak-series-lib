---
title: Motor Limits
description: Per-motor limit definitions and constraints.
---

`include/motors/MotorLimits.hpp` defines the per-motor constraints (position, velocity,
current, and acceleration ranges) used to validate commands before they are encoded.

<!-- TODO: document the MotorLimits structure and the supported AK Series models -->
